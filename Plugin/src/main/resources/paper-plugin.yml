name: Network
version: '${project.version}'
main: net.bteuk.network.Network
api-version: '26.2'

dependencies:
  server:
    WorldGuard:
      load: BEFORE
      required: true
      join-classpath: true
    LuckPerms:
      load: BEFORE
      required: true
      join-classpath: true
    DecentHolograms:
      load: BEFORE
      required: false
      join-classpath: true
    TeachingTutorials:
      load: AFTER
      required: false
      join-classpath: true
    Terraplusminus:
      load: BEFORE
      required: false
      join-classpath: true